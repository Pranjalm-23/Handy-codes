#include <iostream>

using namespace std;

void checkForPaths(int x, int y){
  
}

int main()
{
    
    int noOfInputs;
    cin >> noOfInputs;

    for(int i = 0; i < noOfInputs; i++){
      int x, y;
      cin >> x;
      cin >> y;
      checkForPaths(x, y);
    }

    return 0;
}
